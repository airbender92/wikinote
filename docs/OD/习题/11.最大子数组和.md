# 53. 最大子数组和（Kadane 算法，卡登算法）
> 核心思想：遍历数组，维护**当前子数组和 currSum**
> - 如果 `currSum + nums[i]` 比 `nums[i]` 还小：丢弃前面的子数组，从当前元素重新开始 `currSum = nums[i]`
> - 否则：`currSum += nums[i]`
> - 每次更新全局最大值 `maxSum`

## JS 代码 O(n)，空间 O(1)
```javascript
/**
 * @param {number[]} nums
 * @return {number}
 */
var maxSubArray = function(nums) {
    let currSum = nums[0];
    let maxSum = nums[0];

    for(let i = 1; i < nums.length; i++){
        currSum = Math.max(nums[i], currSum + nums[i]);
        maxSum = Math.max(maxSum, currSum);
    }
    return maxSum;
};
```

## 测试用例
| nums | 预期输出 |
|---|---|
| `[-2,1,-3,4,-1,2,1,-5,4]` | 6 |
| `[1]` | 1 |
| `[5,4,-1,7,8]` | 23 |
| `[-3,-2,-1]` | -1 |

## 测试代码
```js
console.log(maxSubArray([-2,1,-3,4,-1,2,1,-5,4])); //6
console.log(maxSubArray([1])); //1
console.log(maxSubArray([5,4,-1,7,8])); //23
console.log(maxSubArray([-3,-2,-1])); //-1
```

## 进阶：分治法 O(n log n)（了解，面试优先写Kadane）
思路：数组分成左右两半，最大子数组只能是三种情况：左半最大 / 右半最大 / 跨中点。
```javascript
var maxSubArray = function(nums) {
    function getMax(l, r){
        if(l === r) return nums[l];
        const mid = (l + r) >> 1;
        const leftMax = getMax(l, mid);
        const rightMax = getMax(mid+1, r);

        // 跨中点
        let crossLeft = -Infinity;
        let sum = 0;
        for(let i = mid; i >= l; i--){
            sum += nums[i];
            crossLeft = Math.max(crossLeft, sum);
        }
        let crossRight = -Infinity;
        sum = 0;
        for(let i = mid+1; i <= r; i++){
            sum += nums[i];
            crossRight = Math.max(crossRight, sum);
        }
        const crossMax = crossLeft + crossRight;
        return Math.max(leftMax, rightMax, crossMax);
    }
    return getMax(0, nums.length -1);
};
```

### 面试重点
1. Kadane 本质贪心：前面一段和如果是负数，就舍弃，重新开始子数组
2. 边界：数组全是负数，必须选一个，不能返回0
3. 时间O(n)，一次遍历，最优解法

要不要继续下一题，比如 226.翻转二叉树？还是继续数组类？