# 3. 无重复字符的最长子串（滑动窗口 + 哈希表）
> 核心思路：滑动窗口（双指针），用哈希记录字符最近出现下标。窗口 `[left, right]` 内永远无重复字符。
> 当`s[right]`已经在窗口里出现过，直接把左边界跳到重复字符上次位置的下一位。

## JS 代码
```javascript
/**
 * @param {string} s
 * @return {number}
 */
var lengthOfLongestSubstring = function(s) {
    const map = new Map(); // key:字符，value:字符下标
    let maxLen = 0;
    let left = 0;

    for(let right = 0; right < s.length; right++){
        const char = s[right];
        // 如果字符存在，并且它的下标 >= left（说明在当前窗口内重复）
        if(map.has(char) && map.get(char) >= left){
            left = map.get(char) + 1;
        }
        map.set(char, right);
        maxLen = Math.max(maxLen, right - left + 1);
    }
    return maxLen;
};
```

## 测试用例
| 输入s | 预期输出 | 说明 |
| ---- | ---- | ---- |
| `"abcabcbb"` | 3 | 最长`abc` |
| `"bbbbb"` | 1 | 单个b |
| `"pwwkew"` | 3 | `wke` |
| `""` | 0 | 空字符串 |
| `" "` | 1 | 单个空格 |
| `"au"` | 2 | `au` |
| `"abba"` | 2 | 边界坑，重点测试 |

## 测试执行
```js
console.log(lengthOfLongestSubstring("abcabcbb")); // 3
console.log(lengthOfLongestSubstring("bbbbb"));    // 1
console.log(lengthOfLongestSubstring("pwwkew"));   // 3
console.log(lengthOfLongestSubstring(""));         // 0
console.log(lengthOfLongestSubstring(" "));        // 1
console.log(lengthOfLongestSubstring("au"));       // 2
console.log(lengthOfLongestSubstring("abba"));     // 2
```

### 复杂度
- 时间：$\(O(n)\)$，每个字符最多访问一次
- 空间：字符集有限，$\(O(1)\)$

如果你需要 Python / Java / Go 版本代码，我可以一并给出。