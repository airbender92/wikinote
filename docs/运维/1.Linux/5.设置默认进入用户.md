1. 回到 Windows PowerShell 执行(直接以 root 身份登录，**不需要密码**，进来就可以改 `/etc/wsl.conf`):
   `wsl -d Ubuntu -u root`

2. 在 root 里面编辑:
   `nano /etc/wsl.conf`

3. 写入:

```
[user]
default=yambo

```

4. `Ctrl+O` 回车保存，`Ctrl+X`退出。

5. 执行完，powershell 执行关闭 wsl:
   `wsl --shutdown`

6. 重进 wsl，默认就是 yambo
