```sh
# 直接启动你的 Ubuntu 进去之后提示符是 `$`，就是完整 Ubuntu
wsl -d Ubuntu

# 把默认 WSL 改成 Ubuntu（以后直接敲 wsl 就进 Ubuntu，不再跳进 docker-desktop）
wsl --set-default Ubuntu

# 列出你电脑上**所有已经安装注册的 WSL Linux 子系统**
wsl --list --verbose
```
