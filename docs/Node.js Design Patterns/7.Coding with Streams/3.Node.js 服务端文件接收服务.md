```js
 import { createServer } from 'http'
import { createWriteStream } from 'fs'
import { createGunzip } from 'zlib'
import { basename, join } from 'path'
const server = createServer((req, res) => {
  const filename = basename(req.headers['x-filename'])
  const destFilename = join('received_files', filename)
  console.log(`File request received: ${filename}`)
  req
    .pipe(createGunzip())
    .pipe(createWriteStream(destFilename))
    .on('finish', () => {
      res.writeHead(201, { 'Content-Type': 'text/plain' })
      res.end('OK\n')
      console.log(`File saved: ${destFilename}`)
    })
})
server.listen(3000, () => console.log('Listening on http://localhost:3000'))
```

这是一个 **Node.js 服务端文件接收服务**：
- 监听 3000 端口
- 接收客户端上传的 **gzip 压缩文件流**
- 自动解压
- 保存到本地 `received_files/xxx`
- 响应客户端 OK

---

# 逐行解读

```ts
import { createServer } from 'http'
```
- 从 Node.js 内置 `http` 模块导入 **创建 HTTP 服务** 的方法
- 用来启动一个 HTTP 服务器，接收请求

```ts
import { createWriteStream } from 'fs'
```
- 从文件系统模块导入 **可写流**
- 作用：把收到的数据**一点点写入本地文件**，不会吃满内存

```ts
import { createGunzip } from 'zlib'
```
- 从压缩模块导入 **gunzip 解压流**
- 作用：把客户端传过来的 gzip 压缩数据自动解压

```ts
import { basename, join } from 'path'
```
- `basename`：获取文件名（去掉路径，只留最后一段）
- `join`：安全拼接路径，避免 `/` 错误
- 都是路径处理工具

---

```ts
const server = createServer((req, res) => {
```
- 创建 HTTP 服务
- 每进来一个请求，就执行这个回调
- `req`：请求对象（包含上传的数据、请求头）
- `res`：响应对象（用来返回结果给客户端）

---

```ts
const filename = basename(req.headers['x-filename'])
```
- 从**请求头 `x-filename`** 里拿到客户端传的文件名
- 用 `basename()` 过滤掉路径，只保留文件名
- 目的：防止客户端传类似 `../../etc/passwd` 进行路径穿越攻击

```ts
const destFilename = join('received_files', filename)
```
- 把文件名拼接到 `received_files/` 目录下
- 最终类似：`received_files/abc.txt`

```ts
console.log(`File request received: ${filename}`)
```
- 打印日志：收到文件

---

```ts
req
  .pipe(createGunzip())
  .pipe(createWriteStream(destFilename))
```
这是核心流操作：

1. **`req` 是请求的可读流**
   客户端上传的文件数据，从这里源源不断过来

2. **`.pipe(createGunzip())`**
   数据先经过 **gzip 解压**
   客户端传的是压缩包，服务端自动解压

3. **`.pipe(createWriteStream(destFilename))`**
   解压后的数据直接写入目标文件
   用流的方式，**边传边写，不会占用大量内存**

> `.pipe()` = 管道：把前一个流的输出，自动接到下一个流的输入

---

```ts
.on('finish', () => {
```
- 监听**写入完成事件**
- 当文件全部写入磁盘后触发

```ts
res.writeHead(201, { 'Content-Type': 'text/plain' })
```
- 返回 HTTP 状态码 201 Created
- 返回内容类型纯文本

```ts
res.end('OK\n')
```
- 给客户端回复：OK

```ts
console.log(`File saved: ${destFilename}`)
```
- 打印：文件已保存

---

```ts
})
```
关闭 createServer 的回调

---

```ts
server.listen(3000, () => console.log('Listening on http://localhost:3000'))
```
- 服务监听 3000 端口
- 启动成功后打印日志

---

# 整体流程总结（超清晰）
1. 客户端上传文件，并在请求头 `x-filename` 带上文件名
2. 服务端接收**数据流**
3. 自动 gzip 解压
4. 写入本地 `received_files/xxx`
5. 写完后返回 201 OK
6. 全程流式处理，大文件也不会爆内存

---

# 典型客户端调用示例（curl）
```bash
curl -X POST -H "x-filename: data.txt.gz" \
  --data-binary @data.txt.gz \
  http://localhost:3000
```
服务端会自动解压并保存为 `received_files/data.txt`

---

# 你可能想问的关键点
## 1. 为什么用流（pipe）而不是读完全部再写？
- 大文件不会占满内存
- 一边接收一边写入，效率高
- 适合大文件上传

## 2. 为什么要 basename？
防路径穿越攻击：
客户端传 `x-filename: "../../../etc/passwd"`
basename 会变成 `passwd`，不会写到系统目录

## 3. createGunzip 干什么？
客户端传的是 `.gz` 压缩文件，服务端自动解压成原文件

## 4. 这是一个极简但非常标准的文件上传服务
企业里的大文件上传服务，底层都是这个思路。

---

如果你需要，我可以再给你写：
- 对应的**客户端上传代码**
- 加上错误处理（catch error）
- 加上目录不存在自动创建
- 加上文件覆盖校验

----

```js
import { request } from 'http'
import { createGzip } from 'zlib'
import { createReadStream } from 'fs'
import { basename } from 'path'
const filename = process.argv[2]
const serverHost = process.argv[3]
const httpRequestOptions = {
  hostname: serverHost,
  port: 3000,
  path: '/',
  method: 'PUT',
  headers: {
    'Content-Type': 'application/octet-stream',
    'Content-Encoding': 'gzip',
    'X-Filename': basename(filename)
  }
}
const req = request(httpRequestOptions, (res) => {
  console.log(`Server response: ${res.statusCode}`)
})
createReadStream(filename)
  .pipe(createGzip())
  .pipe(req)
  .on('finish', () => {
    console.log('File successfully sent')
  })
```

我给你**逐行完整、通俗、不跳步**讲清楚，这是**上面那段服务端对应的客户端上传代码**。

# 逐行解读

```js
import { request } from 'http'
```
- 从 Node.js 内置 `http` 模块导入 **request**
- 作用：**发起一个 HTTP 请求**（客户端用）

```js
import { createGzip } from 'zlib'
```
- 创建 **gzip 压缩流**
- 把文件压缩后再上传，节省流量

```js
import { createReadStream } from 'fs'
```
- 创建**文件可读流**
- 从本地磁盘读取文件，用流的方式一点点读

```js
import { basename } from 'path'
```
- 取文件名（去掉路径，只保留最后的名字）
- 传给服务端，告诉它文件叫什么

---

```js
const filename = process.argv[2]
```
- 从**命令行参数**取第 3 个值作为要上传的文件路径
- 例如执行：
  ```
  node client.js test.txt localhost
  ```
  那么 `filename = 'test.txt'`

```js
const serverHost = process.argv[3]
```
- 取第 4 个参数作为服务器地址
- 上面例子就是 `localhost`

---

```js
const httpRequestOptions = {
  hostname: serverHost,
  port: 3000,
  path: '/',
  method: 'PUT',
  headers: {
    'Content-Type': 'application/octet-stream',
    'Content-Encoding': 'gzip',
    'X-Filename': basename(filename)
  }
}
```
这是**请求配置**：

- `hostname: serverHost` → 服务器地址
- `port: 3000` → 端口 3000
- `path: '/'` → 请求根路径
- `method: 'PUT'` → 使用 PUT 方法上传文件

请求头：
- `Content-Type: application/octet-stream` → 二进制流
- `Content-Encoding: gzip` → 告诉服务器：我发的是 gzip 压缩数据
- `X-Filename` → 自定义头，把**纯文件名**传给服务端

---

```js
const req = request(httpRequestOptions, (res) => {
  console.log(`Server response: ${res.statusCode}`)
})
```
- 发起请求
- 当服务器**返回响应**时，打印状态码（比如 201）

---

```js
createReadStream(filename)
  .pipe(createGzip())
  .pipe(req)
  .on('finish', () => {
    console.log('File successfully sent')
  })
```
这是**核心流逻辑**：

1. `createReadStream(filename)`
   读取本地文件，以流的形式输出

2. `.pipe(createGzip())`
   把文件流**压缩成 gzip**

3. `.pipe(req)`
   把压缩后的数据流**直接发送到服务器**
   → 流式上传，不占内存

4. `.on('finish')`
   当**全部数据发送完成**时触发
   打印：文件上传成功

---

# 整体流程一句话总结
客户端：
读取本地文件 → gzip 压缩 → 流式上传到服务端 → 发送成功打印日志

服务端（你上一段代码）：
接收流 → gunzip 解压 → 写入文件 → 返回 201

---

# 运行示例
先启动服务端：
```bash
node server.js
```

再运行客户端：
```bash
node client.js test.txt localhost
```

结果：
- 本地 `test.txt` 被压缩上传
- 服务端解压保存到 `received_files/test.txt`
- 两端都会打印日志

---

# 关键点你要知道
- 全程**流式处理**，大文件不会爆内存
- 客户端压缩、服务端自动解压，一对配套使用
- 用自定义头 `X-Filename` 传递文件名
- `pipe()` 会自动控制数据流速，不会拥堵

如果你愿意，我可以帮你：
- 加上错误捕获
- 加上进度条
- 或者改成 Express / Koa 版本