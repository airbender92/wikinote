P265

# 工厂模式（Factory）
工厂模式是 **Node.js 中最常用的设计模式之一**，核心就是：**用函数来创建对象，而不是直接用 new**。

## 核心作用
1. **解耦对象创建与使用**
   调用方只管要对象，不用管内部怎么 new、依赖什么。

2. **运行时动态决定创建哪种对象**
   根据配置、环境、参数，自动返回不同实例。

3. **接口更小、更安全**
   工厂只是一个函数，不像 class 可以被继承、修改，更稳定。

4. **借助闭包实现真正封装**
   可以隐藏内部状态，只暴露需要的方法。

---

## 最简示例
```js
// 工厂函数
function createUser(name, role) {
  return {
    name,
    role,
    greet() {
      return `Hi, I'm ${name}, a ${role}`
    }
  }
}

// 使用
const admin = createUser('Alice', 'admin')
const guest = createUser('Bob', 'guest')
```

这就是工厂：**一个函数，返回对象**。

---

## Node.js 里的典型用法
### 1. 根据类型创建不同实例
```js
function createLogger(type) {
  if (type === 'file') {
    return new FileLogger()
  }
  if (type === 'console') {
    return new ConsoleLogger()
  }
}

const logger = createLogger('file') // 调用方不用关心具体类
```

### 2. 隐藏构造细节，简化 API
```js
function createServer(port) {
  const server = http.createServer()
  server.listen(port)
  return server
}

// 外部直接用，不用关心 listen、初始化逻辑
const server = createServer(3000)
```

### 3. 闭包封装私有变量
```js
function createCounter() {
  let count = 0 // 真正私有

  return {
    inc() { count++ },
    get() { return count }
  }
}

const counter = createCounter()
counter.inc()
console.log(counter.get()) // 1
// count 外部完全访问不到
```

---

## 一句话记住
**工厂 = 造对象的函数**
好处：灵活、封装、安全、调用方简单。

Node.js 内置模块里到处都是工厂，比如：
- `fs.createReadStream()`
- `http.createServer()`
- `crypto.createHash()`

它们全是工厂模式。

----
# 解耦对象创建与具体实现
工厂模式最核心的价值，就是**把“要什么对象”和“怎么造这个对象”分开**。

## 为什么不用直接 `new`？
直接 `new Image(name)` 看上去简单，但有一个硬伤：
**你的代码和 `Image` 这个具体类强绑定了**。

一旦底层重构，比如把 `Image` 拆成：
- `ImageJpeg`
- `ImageGif`
- `ImagePng`

所有直接 `new Image` 的地方**全部要改**，代码完全不灵活。

---

## 工厂模式如何解决？
对外只暴露一个工厂函数，内部怎么实现全隐藏：

```js
// 对外接口不变
function createImage(name) {
  // 内部逻辑可以随便改
  if (name.match(/\.jpe?g$/)) {
    return new ImageJpeg(name)
  } else if (name.match(/\.gif$/)) {
    return new ImageGif(name)
  } else if (name.match(/\.png$/)) {
    return new ImagePng(name)
  } else {
    throw new Error('Unsupported format')
  }
}
```

外部调用代码**一行不用改**：
```js
const image = createImage('photo.jpeg')
```

这就是**解耦**：
- 上层：只关心要一个 image
- 下层：工厂内部可以随意重构、替换实现

---

## 额外好处：隐藏类，缩小公开接口
在 Node.js 模块里，你可以：
- 不导出 `ImageJpeg` / `ImageGif` 等类
- **只导出工厂函数**

外部完全拿不到具体类，也就不能：
- 随意继承
- 直接修改原型
- 绕过你预设的创建逻辑

这就是书中说的 **small surface area（小公开接口）**，更安全、更易维护。

---

## 一句话总结
- **直接 `new` = 写死实现**
- **工厂 = 封装创建逻辑，随时可换底层实现**

这是工厂模式在 Node.js 里无处不在的根本原因。