# 核心解释（简洁完整版）
**/var/log/nginx/ 是 NGINX 默认存放日志的目录**，里面有两个关键日志文件：
- **access.log**：记录**所有用户请求**，谁访问了、访问了什么、返回什么状态，每条请求一行。
- **error.log**：记录**错误信息、启动失败、配置报错、服务异常**，开启 debug 模块后还会输出调试详情。

简单说：
**看访问记录用 access.log，查故障、报错用 error.log。**