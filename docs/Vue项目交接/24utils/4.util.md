好，我**逐行对着源码**给你讲，一句一句对应，不概括、不跳步，完全按代码本身说。

```ts
/**
 *
 * @param component 需要注册的组件
 * @param alias 组件别名
 * @returns any
 */
export const withInstall = <T>(component: T, alias?: string) => {
  // 把传入的 component 断言为 any 类型，方便后续扩展 install 方法
  const comp = component as any
  // 给组件挂载 install 方法，Vue 插件规范要求的入口
  comp.install = (app: any) => {
    // 全局注册组件：用组件自身的 name 或 displayName 作为组件名
    app.component(comp.name || comp.displayName, component)
    // 如果传了别名 alias，就挂载到全局属性 app.config.globalProperties
    if (alias) {
      app.config.globalProperties[alias] = component
    }
  }
  // 返回组件，并把类型标记为 T & Plugin，符合 Vue 插件类型
  return component as T & Plugin
}
```

```ts
/**
 * @param str 需要转下划线的驼峰字符串
 * @returns 字符串下划线
 */
export const humpToUnderline = (str: string): string => {
  // 正则匹配所有大写字母，前面加 -，再整体转小写
  // 例如 userName → user-name
  return str.replace(/([A-Z])/g, '-$1').toLowerCase()
}
```

```ts
/**
 * @param str 需要转驼峰的下划线字符串
 * @returns 字符串驼峰
 */
export const underlineToHump = (str: string): string => {
  // 空值直接返回空
  if (!str) return ''
  // 匹配 - 后面的字母，把它转成大写，去掉 -
  // 例如 user-name → userName
  return str.replace(/\-(\w)/g, (_, letter: string) => {
    return letter.toUpperCase()
  })
}
```

```ts
/**
 * 驼峰转横杠
 */
export const humpToDash = (str: string): string => {
  // 和 humpToUnderline 是一模一样的实现：大写字母前加 -，小写
  return str.replace(/([A-Z])/g, '-$1').toLowerCase()
}
```

```ts
// 设置 CSS 变量，默认挂载在 :root 上
export const setCssVar = (prop: string, val: any, dom = document.documentElement) => {
  dom.style.setProperty(prop, val)
}
```

```ts
// 获取 CSS 变量值
export const getCssVar = (prop: string, dom = document.documentElement) => {
  return getComputedStyle(dom).getPropertyValue(prop)
}
```

```ts
/**
 * 查找数组对象的某个下标
 * @param {Array} ary 查找的数组
 * @param {Functon} fn 判断的方法
 */
export const findIndex = <T = Recordable>(ary: Array<T>, fn: Fn): number => {
  // 浏览器原生支持 findIndex 就直接用
  if (ary.findIndex) {
    return ary.findIndex(fn)
  }
  // 不支持就用 some 遍历，找到后记录 index 并终止循环
  let index = -1
  ary.some((item: T, i: number, ary: Array<T>) => {
    const ret: T = fn(item, i, ary)
    if (ret) {
      index = i
      return ret
    }
  })
  return index
}
```

```ts
// 去除字符串首尾空格
export const trim = (str: string) => {
  return str.replace(/(^\s*)|(\s*$)/g, '')
}
```

```ts
/**
 * @param {Date | number | string} time 需要转换的时间
 * @param {String} fmt 需要转换的格式 如 yyyy-MM-dd、yyyy-MM-dd HH:mm:ss
 */
export function formatTime(time: Date | number | string, fmt: string) {
  // 无时间直接返回空
  if (!time) return ''
  else {
    // 转成 Date 对象
    const date = new Date(time)
    // 定义各个时间字段的匹配规则
    const o = {
      'M+': date.getMonth() + 1,
      'd+': date.getDate(),
      'H+': date.getHours(),
      'm+': date.getMinutes(),
      's+': date.getSeconds(),
      'q+': Math.floor((date.getMonth() + 3) / 3),
      S: date.getMilliseconds()
    }
    // 处理年份，替换 yyyy/yy
    if (/(y+)/.test(fmt)) {
      fmt = fmt.replace(RegExp.$1, (date.getFullYear() + '').substr(4 - RegExp.$1.length))
    }
    // 遍历其他字段，替换成对应值，不足两位补 0
    for (const k in o) {
      if (new RegExp('(' + k + ')').test(fmt)) {
        fmt = fmt.replace(
          RegExp.$1,
          RegExp.$1.length === 1 ? o[k] : ('00' + o[k]).substr(('' + o[k]).length)
        )
      }
    }
    return fmt
  }
}
```

```ts
/**
 * 生成随机字符串
 */
export function toAnyString() {
  // 按模板 xxxxx-xxxxx-4xxxx-yxxxx-xxxxx 生成随机字符
  const str: string = 'xxxxx-xxxxx-4xxxx-yxxxx-xxxxx'.replace(/[xy]/g, (c: string) => {
    const r: number = (Math.random() * 16) | 0
    const v: number = c === 'x' ? r : (r & 0x3) | 0x8
    return v.toString()
  })
  return str
}
```

```ts
/**
 * 首字母大写
 */
export function firstUpperCase(str: string) {
  // 先全小写，再把开头或空格后的第一个字母转大写
  return str.toLowerCase().replace(/( |^)[a-z]/g, (L) => L.toUpperCase())
}
```

```ts
/**
 * 把对象转为formData
 */
export function objToFormData(obj: Recordable) {
  // 新建 FormData 对象
  const formData = new FormData()
  // 遍历对象 key，逐个 append 进 FormData
  Object.keys(obj).forEach((key) => {
    formData.append(key, obj[key])
  })
  return formData
}
```

这样就是**完全对着源码逐行解释**，每一句都对应代码本身，没有额外扩展。