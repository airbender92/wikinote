# 接手 istoxx 项目 行动步骤（适配你本地 VSCode + Continue AI Agent）
## 第一步：前置信息确认（先把资料补齐，不要直接拉代码）
1. **获取权限**
   - 联系交接人xx，申请 Git 仓库访问权限：`xx`
   - 确认内网访问策略：该地址是内网Git仓库，确认你的电脑能连通 `10.27.3.1xx:8888`
   - 索要：项目环境配置文件（`.env` 系列本地环境变量模板）、后端测试环境地址、账号测试权限

2. **完整通读整篇交接文档**
   重点锁定这几块：
   - 5 环境配置与差异
   - 6 本地开发与验证（依赖版本、启动命令）
   - 9 高风险点建议（避坑最重要）

## 第二步：本地环境准备 + 拉取代码
1. 确认本机 Node 版本、包管理器（文档内会写明，Vue3+Vite项目，严格对齐文档指定版本，不要随意升级）
2. 克隆仓库到本地 VSCode
```bash
git clone http://10.xx:8888/istorm-dr/istox.git
cd istormxx
```
3. 在 VSCode 打开项目文件夹

## 第三步：利用 Continue AI 完成项目快速摸底（核心，你自带AI工具优势）
打开 Continue AI Agent，依次下发下面的提示词，自动梳理项目结构：
### 提示词1（项目架构扫描）
> 这是 istorm-boc-ui，Vue3 + Vite + TS 项目，灾备管控系统前端，Hash路由，前后端分离。
> 请扫描整个项目目录，输出：
> 1. 目录结构划分（路由、store、接口请求、组件、权限模块位置）
> 2. 环境变量读取位置、请求封装axios/ws文件路径
> 3. 动态路由、权限控制代码入口
> 4. Nginx Hash路由相关处理代码

### 提示词2（启动流程排查）
> 根据 package.json 脚本、vite配置文件，梳理项目启动、构建执行链路，列出开发启动命令、打包命令，标注环境文件加载逻辑。

### 提示词3（风险梳理）
> 结合交接文档信息，找出项目高风险代码：websocket处理、权限拦截器、环境差异化逻辑、路由守卫，标记容易出bug的位置。

## 第四步：本地安装依赖 + 启动验证
1. 使用文档指定包管理命令安装依赖（npm / pnpm / yarn，文档6.1章节会写）
2. 放入交接人提供的环境变量文件 `.env.development`
3. 执行本地启动命令，验证页面正常打开、接口正常联调测试环境后端
> 如果启动报错，直接把报错日志丢给 Continue AI，让它分析修复方案

## 第五步：验证构建流程
执行打包构建命令，确认产物正常生成，提前熟悉 Jenkins 发布流程（文档第7章节）

## 最简执行顺序总结
1. 拿仓库权限 + 环境变量文件
2. 克隆代码，VSCode打开项目
3. **Continue AI 扫描项目架构，快速熟悉代码组织**
4. 配置环境变量，安装依赖，本地启动跑通页面
5. 通读文档高风险点，记录历史坑
6. 测试打包、理解发布链路

## 额外建议
你可以直接把这份交接文档全文复制，粘贴进 Continue AI，指令：
> 基于这份istorm-boc-ui交接文档，结合源码，生成一份接手项目自检清单，同时标注接手初期最容易踩坑的位置。

如果你需要，我可以帮你整理一份可以直接复制丢给Continue AI的全套提示词合集。