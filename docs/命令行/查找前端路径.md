完美！**你服务器上正在运行的前端/Node服务就是这个**，我直接给你定位到**目录、启动方式、运行状态**，一步到位。

# 一、正在运行的前端/Node服务 精准定位
你运行的命令结果里，**有效进程就是这 3 个**：
```bash
node /usr/local/bin/pnpm start
sh -c node dist/index.js
node dist/index.js
```
这是一个**已经打包构建好的前端/Node项目**，正在后台稳定运行。

---

# 二、立刻找到它的【项目根目录】
执行这条命令，**直接显示代码所在文件夹**（复制运行即可）：
```bash
ls -l /proc/1234/cwd
```

输出会像这样：
```
lrwxrwxrwx 1 root root 0 May ... /proc/1234/cwd -> /你/的/前端/项目/目录
```
`->` 后面的路径，**就是你正在运行的前端项目根目录**。

---

# 三、查看它的完整启动命令（确认项目）
```bash
ps -ef | grep 1234
```
能看到：
- 运行的文件：`dist/index.js`（打包后的前端/服务入口）
- 运行目录：就是上面查到的路径

---

# 四、进入项目目录（直接复制用）
```bash
cd 上面查到的项目路径
```

进去后你会看到：
```
dist/       # 打包好的前端代码
package.json
node_modules/
...
```

---

# 五、这个服务的端口是多少？（查前端访问地址）
```bash
ss -tlnp | grep 1234
```
就能看到它监听的端口（比如 3000/5173/8080 等），就是**前端访问地址**。

---

### 总结（最关键信息）
1. **你服务器上确实在运行前端/Node服务**，进程 PID：`1234`
2. 它是用 `pnpm start` 启动的，运行的是打包后的 `dist/index.js`
3. 用这条命令 **1秒找到项目目录**：
   ```bash
   ls -l /proc/1234/cwd
   ```

把这条命令的输出发给我，我可以直接告诉你**前端访问地址、如何重启、如何停止**。

----


对！**你说得完全正确！**
这个前端不是直接跑在服务器上的，而是**跑在 Docker 容器里**的！所以 `/app` 是容器内部目录，服务器上找不到。

我现在 **1 分钟帮你把整个前端位置、容器、目录、端口全部找出来**，一步都不用猜。

---

# 一、先确认：你的前端确实在 Docker 容器中
你看到的
```
node dist/index.js
/app 目录
```
这是**标准的前端/Node 容器运行方式**！

---

# 二、立刻找到运行前端的那个容器（复制执行）
```bash
docker ps | grep node
```
或者
```bash
docker ps
```

你会看到类似这样的结果：
```
CONTAINER ID   IMAGE         STATUS         PORTS                    NAMES
abc123         node:latest   Up 2 months    0.0.0.0:3000->3000/tcp   frontend-app
```

### 拿到 2 个关键信息：
1. **CONTAINER ID 容器ID**（例如 abc123）
2. **PORTS 前端端口**（例如 3000）

---

# 三、进入容器内部查看前端代码（真正的 /app 目录）
把下面的 `容器ID` 换成你查到的：
```bash
docker exec -it 容器ID sh
```

进入后你就会看到：
```
/app #
```

这就是**真正的前端根目录**！

查看目录：
```
ls -lah
```

---

# 四、查看容器里前端的端口（访问地址）
```bash
docker port 容器ID
```
会显示：
```
3000/tcp -> 0.0.0.0:3000
```
这就是**前端访问端口**！

---

# 五、你只需要发给我这一条结果，我直接告诉你全部信息
```bash
docker ps
```

我会马上告诉你：
- 前端容器名字
- 前端端口
- 前端目录
- 如何重启
- 如何访问

---

### 总结
✅ **你的前端运行在 Docker 容器里**
✅ 服务器上没有 `/app`，容器里才有
✅ 用 `docker ps` 就能找到它
✅ 用 `docker exec -it 容器ID sh` 就能进入前端目录

你把 `docker ps` 的结果发我，我直接帮你定位完整路径！