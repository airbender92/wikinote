我来直接排查。先并行查三件事：dev server 是否在跑、监听在哪个地址、本机 IP 是多少。
服务其实**正在运行且监听正常**（`0.0.0.0:5173`，进程 12440），本机 IP 能看到是 `192.168.1.3`。那问题多半出在 IP 选择、防火墙或网络隔离上。继续确认：
服务端一切正常：**`http://192.168.1.3:5173` 本机访问返回 HTTP 200**，且监听的是 `0.0.0.0`（已对局域网开放）。所以问题锁定在"电脑 → 手机"这条链路。查一下 Windows 防火墙状态：
找到根因了。防火墙配置暴露了问题：

- **当前网络配置文件是「公用」（Public）**，策略为 `BlockInbound, AllowOutbound`——**默认阻止所有入站连接**
- **`InboundUserNotification: 禁用`**——所以 node 首次监听 5173 时系统根本没弹窗问你是否允许，直接静默拦截
- 且入站规则里**没有任何放行 node/5173 的规则**

也就是说：dev server 本身没问题（本机回环访问不受入站拦截影响，所以 curl 200），但**手机从局域网进来时被防火墙拦掉了**。我来尝试添加一条仅放行 5173 端口的入站规则：
需要管理员权限，我这边无法静默提权。先把这个排障结论记入项目日志，然后给你手动操作指引：

## 排查结论

**项目本身完全正常**，问题出在 Windows 防火墙：

| 检查项               | 结果                                                       |
| -------------------- | ---------------------------------------------------------- |
| dev server 状态      | ✅ 运行中，监听 `0.0.0.0:5173`（进程 12440）               |
| 局域网地址           | ✅ `192.168.1.3`（WLAN，网关 192.168.1.1）                 |
| 本机经局域网 IP 访问 | ✅ HTTP 200                                                |
| 防火墙配置文件       | ❌ **「公用」网络，策略 `BlockInbound`，且入站通知被禁用** |
| 放行规则             | ❌ 无任何 node/5173 入站规则                               |

**根因**：你的网络处于 Windows「公用」配置文件下，默认**阻止所有入站连接**，且入站询问通知被禁用（GPO 策略），所以 node 监听 5173 时系统从未弹窗询问，手机的访问请求直接被静默丢弃。本机 curl 走回环所以正常，手机来自局域网就被拦。

## 你需要手动操作（需管理员权限，我无法提权）

**方案 1：只放行 5173 端口（推荐，最精准）**

1. 按 `Win` 键 → 输入 `cmd` → 右键 **以管理员身份运行**
2. 执行：

```
netsh advfirewall firewall add rule name="Vite Dev 5173" dir=in action=allow protocol=TCP localport=5173
```

3. 立即生效，手机重新访问 `http://192.168.1.3:5173`

**方案 2：把当前 WiFi 改为「专用网络」**（家庭/办公可信网络时）
设置 → 网络和 Internet → WLAN → 已连接网络 → 网络配置文件类型 → 改为「专用」

**方案 3：绕开路由器限制**（如果前面加规则不生效或公司网络管控严格）
手机开热点 → 电脑连手机热点 → 重新 `ipconfig` 查电脑新 IP（热点网段通常是 192.168.43.x）→ 手机访问 `http://电脑新IP:5173`

## 验证小技巧

手机上先打开 `http://192.168.1.1`（路由器网关）——能开说明手机网络通；再开 `http://192.168.1.3:5173`。前者通后者不通，就 100% 是电脑侧防火墙/隔离问题。

**注意**：务必用 `http://` 前缀（不是 https），并确认手机和电脑连的是**同一个路由器 WiFi**。执行完方案 1 后告诉我，我帮你再确认一遍端口放行状态。
